# Credentials. Only the *.example files belong in git.
.env.*
!.env.example
!.env.publish.example

# Saved login session from tests/auth.setup.ts
.auth/

node_modules/
playwright-report/
test-results/
allure-results/
allure-report/

# Evidence bundles (EVIDENCE=true). Regenerate when you need them.
evidence/
